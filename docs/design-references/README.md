# Design References

This folder is reserved for local design reference files such as screenshots, saved pages, and downloaded assets.

These files are intentionally ignored by Git because they are large, noisy, and only needed during local design work. Keep reference assets here locally, but do not commit them.
