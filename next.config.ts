import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /*
   * Velite is generated through npm predev/prebuild scripts.
   * The app uses Next 16's default Turbopack build, so the older webpack
   * plugin snippet from Velite's Next.js guide is not the primary hook here.
   */
};

export default nextConfig;
